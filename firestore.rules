rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Baseline rules - Phase 1 will implement full tenant-scoped rules (AUTHZ-1..3)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
